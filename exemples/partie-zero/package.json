{
  "name": "orchestrer-le-code-partie-zero",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "start": "npm run build --silent && node dist/src/index.js",
    "test": "npm run test:etapes --silent && npm run build --silent && node --test dist/test/*.test.js",
    "test:etapes": "node etapes/p01/bonjour.js && node etapes/p02/regles.js && node etapes/p03/index.js fixtures/commande-valide.json && node --test etapes/p04/*.test.js",
    "check": "tsc --noEmit",
    "build": "tsc"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "6.0.3"
  }
}
